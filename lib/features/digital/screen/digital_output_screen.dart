import 'package:flutter/material.dart';

class DigitalOutputScreen extends StatelessWidget {
const DigitalOutputScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}