import 'package:flutter/material.dart';



class DigitalInputScreen extends StatelessWidget {
const DigitalInputScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container(
      child: ElevatedButton(
        onPressed: (){},
        child: Text("DISCONNECT"),
      ),
    );
  }
}